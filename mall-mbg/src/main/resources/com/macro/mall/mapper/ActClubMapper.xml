<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.mapper.ActClubMapper">
  <resultMap id="BaseResultMap" type="com.macro.mall.model.ActClub">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="background_img" jdbcType="VARCHAR" property="backgroundImg" />
    <result column="logo_img" jdbcType="VARCHAR" property="logoImg" />
    <result column="introduce" jdbcType="VARCHAR" property="introduce" />
    <result column="club_pics" jdbcType="VARCHAR" property="clubPics" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    id, name, background_img, logo_img, introduce, club_pics
  </sql>
  <select id="selectByExample" parameterType="com.macro.mall.model.ActClubExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from act_club
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from act_club
    where id = #{id,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from act_club
    where id = #{id,jdbcType=BIGINT}
  </delete>
  <delete id="deleteByExample" parameterType="com.macro.mall.model.ActClubExample">
    delete from act_club
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="com.macro.mall.model.ActClub">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into act_club (name, background_img, logo_img, 
      introduce, club_pics)
    values (#{name,jdbcType=VARCHAR}, #{backgroundImg,jdbcType=VARCHAR}, #{logoImg,jdbcType=VARCHAR}, 
      #{introduce,jdbcType=VARCHAR}, #{clubPics,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.macro.mall.model.ActClub">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into act_club
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="name != null">
        name,
      </if>
      <if test="backgroundImg != null">
        background_img,
      </if>
      <if test="logoImg != null">
        logo_img,
      </if>
      <if test="introduce != null">
        introduce,
      </if>
      <if test="clubPics != null">
        club_pics,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="backgroundImg != null">
        #{backgroundImg,jdbcType=VARCHAR},
      </if>
      <if test="logoImg != null">
        #{logoImg,jdbcType=VARCHAR},
      </if>
      <if test="introduce != null">
        #{introduce,jdbcType=VARCHAR},
      </if>
      <if test="clubPics != null">
        #{clubPics,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="com.macro.mall.model.ActClubExample" resultType="java.lang.Long">
    select count(*) from act_club
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>

    <update id="updateByExampleSelective" parameterType="map">
    update act_club
    <set>
      <if test="record.id != null">
        id = #{record.id,jdbcType=BIGINT},
      </if>
      <if test="record.name != null">
        name = #{record.name,jdbcType=VARCHAR},
      </if>
      <if test="record.backgroundImg != null">
        background_img = #{record.backgroundImg,jdbcType=VARCHAR},
      </if>
      <if test="record.logoImg != null">
        logo_img = #{record.logoImg,jdbcType=VARCHAR},
      </if>
      <if test="record.introduce != null">
        introduce = #{record.introduce,jdbcType=VARCHAR},
      </if>
      <if test="record.clubPics != null">
        club_pics = #{record.clubPics,jdbcType=VARCHAR},
      </if>
    </set>
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update act_club
    set id = #{record.id,jdbcType=BIGINT},
      name = #{record.name,jdbcType=VARCHAR},
      background_img = #{record.backgroundImg,jdbcType=VARCHAR},
      logo_img = #{record.logoImg,jdbcType=VARCHAR},
      introduce = #{record.introduce,jdbcType=VARCHAR},
      club_pics = #{record.clubPics,jdbcType=VARCHAR}
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="com.macro.mall.model.ActClub">
    update act_club
    <set>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="backgroundImg != null">
        background_img = #{backgroundImg,jdbcType=VARCHAR},
      </if>
      <if test="logoImg != null">
        logo_img = #{logoImg,jdbcType=VARCHAR},
      </if>
      <if test="introduce != null">
        introduce = #{introduce,jdbcType=VARCHAR},
      </if>
      <if test="clubPics != null">
        club_pics = #{clubPics,jdbcType=VARCHAR},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.macro.mall.model.ActClub">
    update act_club
    set name = #{name,jdbcType=VARCHAR},
      background_img = #{backgroundImg,jdbcType=VARCHAR},
      logo_img = #{logoImg,jdbcType=VARCHAR},
      introduce = #{introduce,jdbcType=VARCHAR},
      club_pics = #{clubPics,jdbcType=VARCHAR}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="listByClubIds" resultType="com.macro.mall.model.dto.ActClubDto">
    select c.id,
           name,
           background_img as backgroundImg,
           logo_img as logoImg,
           introduce,
           club_pics as clubPics,
           count(1) as fansCnt
    from act_user_club uc
    LEFT JOIN act_club c
         on uc.club_id = c.id
    where club_id in
      <foreach close=")" collection="clubIds" item="clubId" open="(" separator=",">
        #{clubId}
      </foreach>
    group by club_id
  </select>

  <select id="listClubActNums" resultType="com.macro.mall.model.dto.ActClubDto">
    select club_id as id, count(1) as actCnt
    from act_act
    where club_id in
    <foreach close=")" collection="clubIds" item="clubId" open="(" separator=",">
      #{clubId}
    </foreach>
    group by club_id
  </select>

    <select id="getClubInfo" resultType="com.macro.mall.model.dto.ActClubDto">
      select c.id,
             name,
             background_img as backgroundImg,
             logo_img as logoImg,
             introduce,
             club_pics as clubPics,
             count(1) as fansCnt
      from act_user_club uc
      LEFT JOIN act_club c
           on uc.club_id = c.id
      where club_id = #{clubId}
    </select>

  <select id="getClubActCnt" resultType="java.lang.Integer">
    select count(1)
    from act_act
    where club_id = #{clubId}
  </select>

</mapper>